export enum SceneLayerTypeEnum {
  Sprites = 'sprites',
  Frames = 'frames',
  Grounds = 'grounds',
  Grids = 'grids',
  Events = 'events',
}

export enum ExportSceneTypeEnum {
  Simple = 'simple',
  Full = 'full',
}
